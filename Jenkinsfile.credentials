pipeline {
    agent any
    environemnt {
        CREDENTIALS = credentials('ssh_passwd')
    }
    stages {
        stage('Log in'){
            steps {
                sh 'ehco "my username is ${CREDENTIALS_USR}"'
                echo "my password is ${CREDENTIALS_PSW}"
            }
        }
    }
}
